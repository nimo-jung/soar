fastapi==0.96.1
uvicorn[standard]==0.22.0
qdrant-client==1.6.2
# pydantic version must be compatible with FastAPI (<2.0)
pydantic<2.0.0,>=1.10
# NOTE: sentence-transformers is optional. 
#   - If USE_EMBED_MODEL=1 and no ONNX model available, downloads transformers+torch at first run.
#   - If you want a lightweight runtime (no ML), set USE_EMBED_MODEL=0 or remove this line entirely.
sentence-transformers==2.2.2
# onnxruntime CPU-only build (NOT the GPU variant). onnxruntime-gpu is intentionally excluded 
# because this service typically runs without GPU. The CPU build pulls ~350 MB instead of ~1 GB.
onnxruntime-cpu==1.15.1
numpy==1.26.4
